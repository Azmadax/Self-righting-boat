name: self-righting-boa

channels:
  - conda-forge
dependencies:
  - poetry=2.0.0
  - python=3.10
prefix: /home/perso/conda/envs/self-righting-boat

# Non-standard section listing target platforms for conda-lock:
platforms:
  - linux-64
  - osx-arm64
  - osx-64
  - win-64
